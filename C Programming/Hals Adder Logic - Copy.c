#include <stdio.h>
#include <stdlib.h>
#include<math.h>

int main() 
{
    int x, y,sum,carry;
    printf("pleasr enter a two numbers : ");
    scanf("%d", &x);
    while (b != 0)
    {
        sum = a^b;
        carry = a && b << 1;
        a = sum;
        b = carry;
    }
}